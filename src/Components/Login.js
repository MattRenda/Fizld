import React, {useState} from 'react';

const Login = ()=> {

    const handleChange=()=>{

    }
    return (
        <>
            <Button className='header-btn dark-blue' onClick={handleChange}>
                Contact me
            </Button>
        </>
    );
}

export default Login;

